### 一、日志清理任务

* **执行时间**：每天 0 点（0 0 \* \* \*）
* **操作**：运行 `/home/clean_logs.sh`脚本
* **作用**：自动清除 7 天前的日志文件，释放存储空间。

### 二、Amazon 广告及数据同步任务

#### 1. 标签规则生成（自定义标签对象）

* **执行时间**：每 30 分钟一次（\*/30 \* \* \* \*）
* **操作**：进入 `/home/AmazonTask`目录，执行 `python3 cli.py label`
* **作用**：按标签自定义规则定期生成标签对象，保证标签数据时效性。

#### 2. SP 报表同步（含创建、重试、下载）

* **报表创建**：每天 0 点、3 点、8 点、16 点（0 0,3,8,16 \* \* \*）
  操作：`python3 cli.py report sp create`
  作用：定时创建 SP 报表任务。
* **报表重试**：每 10 分钟一次（\*/10 \* \* \* \*）
  操作：`python3 cli.py report sp retry`
  作用：对未成功创建的 SP 报表任务进行重试，确保任务完成。
* **报表下载**：每 5 分钟一次（\*/5 \* \* \* \*）
  操作：`python3 cli.py report sp download`
  作用：及时下载已生成的 SP 报表，保证数据同步及时性。

#### 3. 其他 SP 相关数据同步

* **ASIN 详情数据**：每天 0 点、3 点、8 点、16 点（0 0,3,8,16 \* \* \*）
  操作：`python3 cli.py report sp asin`
  作用：同步 get\_amzn\_asin\_details 数据。
* **listing\_items 数据**：每天 0 点、3 点、8 点、16 点（0 0,3,8,16 \* \* \*）
  操作：`python3 cli.py report sp listing_items`
  作用：同步 listing\_items 数据。
* **库存汇总数据**：每天 0 点（0 0 \* \* \*）
  操作：`python3 cli.py report sp inventory_summaries --days=7`
  作用：同步近 7 天的库存汇总数据。

#### 4. 订单数据同步

* **订单同步**：每天 0 点、3 点、8 点、16 点（0 0,3,8,16 \* \* \*）
  操作：`python3 cli.py report sp order --workers=30`（30 个工作进程）
  作用：批量同步订单数据。
* **订单刷新（按 ID）**：每小时 0 分（0 \* \* \* \*）
  操作：`python3 cli.py report sp refresh_orders`
  作用：根据订单 ID 刷新订单数据，确保订单信息最新。

#### 5. ABA 数据同步（已注释，原计划）

* **原计划执行时间**：每周二 18 点（0 18 \* \* 2）
* **原操作**：`python3 cli.py report sp search_term_report`
* **说明**：当前被注释（未启用），原计划每周二同步 ABA 搜索词报告。

#### 6. AD 报表同步（含创建、重试、下载）

* **日报表创建**：每天 0 点、3 点、8 点、16 点（0 0,3,8,16 \* \* \*）
  操作：`python3 cli.py report ad create`
  作用：创建 AD 日报表。
* **小时报表创建**：每小时 0 分（0 \* \* \* \*）
  操作：`python3 cli.py report ad create_hour`
  作用：创建 AD 小时级报表，更精细的数据统计。
* **报表重试**：每 10 分钟一次（\*/10 \* \* \* \*）
  操作：`python3 cli.py report ad retry`
  作用：重试未成功的 AD 报表任务。
* **报表下载**：每 5 分钟一次（\*/5 \* \* \* \*）
  操作：`python3 cli.py report ad download`
  作用：及时下载已生成的 AD 报表。

#### 7. 用户 ASIN 授权刷新

* **执行时间**：每天 4 点 20 分、8 点 20 分、12 点 20 分、16 点 20 分、20 点 20 分（20 4,8,12,16,20 \* \* \*）
* **操作**：`python3 cli.py refresh_author`
* **作用**：定期刷新用户的 ASIN 授权状态，保证权限有效性。

### 三、系统策略执行任务

* **系统推荐策略**：每 10 分钟一次（\*/10 \* \* \* \*）
  操作：`python3 cli.py strategy recommend`
  作用：实时生成系统推荐策略。
* **分时预算调整**：每小时 0 分（0 \* \* \* \*）
  操作：`python3 cli.py strategy budget_time`
  作用：按时间段调整预算分配。
* **预算累加**：每小时 0 分（0 \* \* \* \*）
  操作：`python3 cli.py strategy budget_add`
  作用：累加计算预算使用情况。
* **智能托管**：每天 17 点（0 17 \* \* \*）
  操作：`python3 cli.py strategy trust`
  作用：执行智能托管策略。

### 四、丁一 ASIN 详情接口同步

* **执行时间**：每天 2 点（0 2 \* \* \*）
* **操作**：`python3 cli.py asin`
* **作用**：同步丁一 ASIN 详情接口数据。

### 五、待处理任务钉钉通知

* **执行时间**：每天 11 点 30 分、17 点 0 分（30 11 \* \* \* 和 00 17 \* \* \*）
* **操作**：进入 `/home/PingCode`目录，执行 `python3 main.py`
* **作用**：定时向钉钉发送待处理任务通知，提醒相关人员处理。

### 补充说明

* 被注释的任务（以 `#`开头）当前未执行，仅保留配置记录（如 Amazon 广告数据同步、ABA 数据同步）。
* 高频任务（如每 5/10/30 分钟）用于保证数据实时性或任务连续性，低频任务（如每天 / 每周）用于批量处理或汇总操作。
